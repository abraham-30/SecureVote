import { Text, View } from "react-native";

export default function ElectionsScreen(){
        return(
        <View className="flex flex-col p-7 gap-8">
            <Text className="text-3xl font-bold">Elections to Vote</Text>
        </View>
    );
}